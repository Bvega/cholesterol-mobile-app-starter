# Data Source

This project’s initial plan data in `src/data/plan_week_01.json` comes from the provided PDF:
- `Plan_Semanal_Bajar_Colesterol.pdf`

The JSON was created to preserve the exact weekly structure (days + meals + advice) and enable a mobile calendar + shopping list.
